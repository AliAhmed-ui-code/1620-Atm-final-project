def deposit(balance, amount):
    float(balance)
    float(amount)
    balance += amount
    return balance
def withdraw(balance, amount):
    float(balance)
    float(balance)
    balance -= amount
    return balance